require "spec_helper"

RSpec.describe Ruby::Search do
  it "has a version number" do
    expect(Ruby::Search::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
